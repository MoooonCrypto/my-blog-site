# 機能テストチェックリスト

## 🔐 認証システム

### 必須要件
- [ ] **未認証状態でシークレットブラウザで`/admin/login`にアクセス** → ログイン画面が表示される
- [ ] **未認証状態で`/admin/dashboard`に直接アクセス** → `/admin/login`にリダイレクトされる
- [ ] **正しい認証情報でログイン** → ダッシュボードに遷移
- [ ] **誤った認証情報でログイン** → エラーメッセージ表示、ログインできない
- [ ] **ログイン後、ブラウザ再読み込み** → ログイン状態が維持される
- [ ] **ログアウト** → `/admin/login`にリダイレクト、再度ダッシュボードにアクセス不可

---

## 📝 Portfolio管理

### データ登録
- [ ] **管理画面から新規Portfolio作成** → DBに保存される
- [ ] **必須項目（title, slug, description）なしで保存** → バリデーションエラー
- [ ] **画像アップロード** → Supabase Storageに保存、URLがDBに記録
- [ ] **technologiesタグ追加** → 配列としてDBに保存
- [ ] **published=false（下書き）で保存** → 管理画面にのみ表示
- [ ] **published=trueで保存** → 公開画面にも表示
- [ ] **既存データ編集** → 変更がDBに反映
- [ ] **データ削除** → DBから削除、画面から消える

### データ表示（公開画面）
- [ ] **`/portfolio`アクセス** → `published=true`のデータのみ表示
- [ ] **ダミーデータは表示されない**
- [ ] **登録した画像が表示される**
- [ ] **登録したtechnologiesが表示される**
- [ ] **`/portfolio/[id]`詳細ページ** → 実際の登録データが表示（モックデータではない）
- [ ] **画像、GitHub URL、Demo URLが正しく表示・リンクされる**

### ホームページ表示
- [ ] **`/`（トップページ）のプロダクトセクション** → 実際のDB データが表示（ダミーデータではない）
- [ ] **画像が表示される**

---

## 🧪 Sandbox管理

### データ登録
- [ ] **管理画面から新規Sandbox作成** → DBに保存される
- [ ] **published=false/true** → 公開制御が機能
- [ ] **編集・削除** → DBに反映

### データ表示（公開画面）
- [ ] **`/sandbox`アクセス** → `published=true`のデータのみ表示
- [ ] **ダミーデータは表示されない**
- [ ] **`/sandbox/[id]`詳細ページ** → 実際の登録データが表示

---

## 📰 Blog管理

### データ登録
- [ ] **管理画面から新規記事作成** → DBに保存される
- [ ] **slugの重複チェック** → 重複時エラー
- [ ] **published=false/true** → 公開制御が機能
- [ ] **tagsの追加** → 配列としてDBに保存
- [ ] **編集・削除** → DBに反映

### データ表示（公開画面）
- [ ] **`/blog`アクセス** → `published=true`の記事のみ表示
- [ ] **ダミーデータは表示されない**
- [ ] **登録したtags, 公開日が表示される**
- [ ] **`/blog/[slug]`詳細ページ** → 実際の記事内容が表示（モックデータではない）

### ホームページ表示
- [ ] **`/`（トップページ）の記事セクション** → 実際のDBデータが表示（ダミーデータではない）

---

## 👤 Profile管理

### データ登録
- [ ] **管理画面からプロフィール編集** → DBに保存される
- [ ] **SNSリンク追加** → DBに保存される
- [ ] **画像アップロード** → Supabase Storageに保存

### データ表示（公開画面）
- [ ] **`/profile`アクセス** → DBから取得したプロフィール情報が表示
- [ ] **ダミーデータは表示されない**

---

## 🎯 総合チェック

- [ ] **全ページでダミーデータが一切表示されない**
- [ ] **全ての画像が正しく表示される（placeholder.comは使われない）**
- [ ] **管理画面で登録したデータが全て公開画面に反映される**
- [ ] **未認証でadmin画面にアクセスできない**

---

## 現在の状態（テスト実施前）

### ❌ 失敗
- 認証: 未認証で`/admin/dashboard`にアクセスできてしまう
- トップページ: ダミーデータが表示されている
- Portfolio詳細: モックデータが表示されている可能性
- 画像: via.placeholder.comが使われている

### ✅ 成功
- Portfolio一覧（管理画面）: 実データ表示
- Portfolio一覧（公開画面）: DB連携済み（最新修正）
- Blog一覧（公開画面）: DB連携済み（最新修正）
